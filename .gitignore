# Bun
node_modules/
package-lock.json
bun.lockb
*.lock
!libraries/**/yarn.lock
!libraries/**/bun.lock

# Generated files
dist/
build/
.turbo/

# Synced version files (generated from root openiap-versions.json)
packages/*/openiap-versions.json
packages/apple/Sources/openiap-versions.json
# libraries/*/openiap-versions.json — symlinks to root, tracked in git

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo
*~
.claude/*local*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# PR preview recordings should be uploaded to GitHub PR attachments, not
# committed to the repository.
.github/pr-previews/

# Testing
coverage/
.nyc_output/

# Environment
.env
.env.local
.env.*.local

# IAPKit credential files downloaded from prod for local testing.
# Apple .p8 keys + Google service-account JSONs must never be
# committed — they'd grant attackers App Store Server API / Play
# Developer API access on the corresponding project.
*.p8
**/service-account*.json
# Catch-all for Google Cloud project keys downloaded as
# `<project-id>-<hash>.json` (the default name from the GCP console).
# Replaces the per-project `martie-c0b27-*.json` pattern that only
# helped one developer's local setup. We cover the two real formats
# the IAM console produces:
#   1. `<project-id>-<5-hex suffix>-<12-hex key id>.json`
#      (project IDs with the GCP-auto-generated 5-char uniqueness suffix)
#   2. `<project-id>-<12-hex key id>.json`
#      (project IDs the operator named themselves, no GCP suffix)
# Pinning the trailing key-id to exactly 12 hex chars keeps legitimate
# config files (`tsconfig-base.json`, `eslint-config.json`, etc.) out
# of the match while still catching every key the console emits.
**/*-[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]-[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f].json
**/*-[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f].json

# Temp
tmp/
temp/
.build

# RAG Agent generated outputs (benchmark comparisons)
scripts/agent/_generated/
scripts/agent/.lancedb/

# Convex codegen — committed because CI doesn't run `convex dev`,
# but the AI assistance subdirectory holds dev-machine state and stays ignored.
packages/kit/convex/_generated/ai/

# ─── Libraries ───

# React Native / Expo
libraries/*/node_modules/
libraries/*/.expo/
libraries/*/example/node_modules/
libraries/*/example-expo/node_modules/
libraries/*/example/.expo/
libraries/*/example-expo/.expo/

# iOS / Xcode
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.xcuserstate
Pods/

# Android
*.apk
*.ap_
local.properties
libraries/*/.gradle/
libraries/*/example/.gradle/
libraries/*/.cxx/

# Flutter
.dart_tool/
.packages
.pub/
pubspec.lock
.flutter-plugins
.flutter-plugins-dependencies

# Godot
.godot/
godot-cpp/
*.import
!libraries/godot-iap/Example/*.import

# KMP
.kotlin/

# Docusaurus (libraries docs)
libraries/*/docs/.docusaurus/
libraries/*/docs/.cache-loader/
libraries/*/docs/build/

# .NET MAUI / dotnet (maui-iap)
# Build outputs — every csproj writes here; tens of thousands of files.
**/bin/
**/obj/
# IDE / Visual Studio scratch
.vs/
*.user
*.suo
*.userprefs
# NuGet artifacts (released via the workflow, not committed)
*.nupkg
*.snupkg
# MSBuild structured logs and binlogs
*.binlog
# .NET workload + local SDK config (machine-specific)
**/.dotnet/
# MAUI XAML hot-reload temp files
*.xaml.lock
# Xamarin / .NET-for-iOS device-deploy intermediates
*.app.dSYM/
*.ipa
*.maui.bundle/
# Resx generated designer code (per-build artifact)
*.resx.designer.cs
