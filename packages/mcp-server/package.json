{
  "name": "@hyodotdev/openiap-mcp-server",
  "version": "0.1.0",
  "description": "Model Context Protocol server for OpenIAP — wires Claude / Cursor / Codex into kit's product, paywall, subscription, and webhook surfaces.",
  "type": "module",
  "private": true,
  "bin": {
    "openiap-mcp": "./dist/index.js"
  },
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p .",
    "lint": "tsc -p . --noEmit",
    "test": "vitest run",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.5"
  }
}
