{{#if description}}
/// {{{description}}}
{{/if}}
public protocol {{name}}: Codable {
{{#each fields}}
{{#if description}}
    /// {{{description}}}
{{/if}}
    var {{propertyName}}: {{type}} { get }
{{/each}}
}
