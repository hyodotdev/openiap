# Debian marks libssl3 in bookworm not-affected by CVE-2026-14456 (the QUIC
# listener flaw was introduced in OpenSSL 3.5), so Trivy no longer reports it
# and the exception it needed is gone. Keep the list empty rather than carrying
# a dead entry.
vulnerabilities: []
