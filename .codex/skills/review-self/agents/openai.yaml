interface:
  display_name: "Review Self"
  short_description: "Review and simplify current work until stable"
  default_prompt: "Use $review-self to review and simplify the current work under KISS and SSOT rules, fix actionable findings, and recheck at the requested interval until stable."
