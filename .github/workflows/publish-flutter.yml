name: "Publish: flutter_inapp_purchase"

on:
  push:
    tags:
      - "flutter-iap-*"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: libraries/flutter_inapp_purchase
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version: "3.x"

      - name: Install dependencies
        run: flutter pub get

      - name: Publish to pub.dev
        run: |
          # Request OIDC token and add as pub.dev credential
          OIDC_TOKEN=$(curl -sLS "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=https://pub.dev" \
            -H "User-Agent: actions/oidc-client" \
            -H "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            | jq -r '.value')
          dart pub token add https://pub.dev --env-var _PUB_TOKEN
          _PUB_TOKEN="$OIDC_TOKEN" dart pub publish --force
