# OpenIAP Context-Aware RAG Agent v2.0 Configuration
# Copy this file to .env and adjust values as needed

# ============================================================================
# Ollama Configuration
# ============================================================================
OLLAMA_URL=http://localhost:11434

# ============================================================================
# Embedding Model (used for knowledge ingestion and retrieval)
# ============================================================================
# Recommended: nomic-embed-text (fast, good quality)
# Alternatives: mxbai-embed-large (higher quality, slower)
#              bge-m3 (multi-lingual support)
EMBEDDING_MODEL=nomic-embed-text

# ============================================================================
# LLM Model (used for code generation)
# ============================================================================
# Recommended: qwen2.5-coder:14b (best balance of speed and quality)
# Alternatives: qwen2.5-coder:32b (higher quality, needs more RAM)
#              phi4 (smaller, faster)
#              deepseek-coder:6.7b (fast, decent quality)
LLM_MODEL=qwen2.5-coder:14b

# ============================================================================
# GitHub PR Creation
# ============================================================================
# Set to "true" to enable automatic PR creation via gh CLI
# Requires: gh CLI installed and authenticated
CREATE_PR=false
