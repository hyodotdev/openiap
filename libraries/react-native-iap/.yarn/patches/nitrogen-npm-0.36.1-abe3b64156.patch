diff --git a/lib/syntax/types/ArrayType.js b/lib/syntax/types/ArrayType.js
index 91aa9c14e448679c38422710f040bffc9663a975..38148692c99f060fe10e3c07cc445d38f58f66f7 100644
--- a/lib/syntax/types/ArrayType.js
+++ b/lib/syntax/types/ArrayType.js
@@ -12,7 +12,11 @@ export class ArrayType {
         return 'array';
     }
     get isEquatable() {
-        return this.itemType.isEquatable;
+        // Defaulted equality on structs containing std::vector can make Swift drop
+        // the vector's CxxRandomAccessCollection conformance (swiftlang/swift#67410).
+        // Equality is only a generated convenience, so keep array-bearing structs
+        // non-equatable until the affected Swift toolchains are no longer supported.
+        return false;
     }
     getCode(language, options) {
         const itemCode = this.itemType.getCode(language, options);
diff --git a/src/syntax/types/ArrayType.ts b/src/syntax/types/ArrayType.ts
index 135076c24d8e1c1569cce64349203c22b5fce9b3..7077f6f3fb2fd6101dfc440cb28a42470308f500 100644
--- a/src/syntax/types/ArrayType.ts
+++ b/src/syntax/types/ArrayType.ts
@@ -18,7 +18,11 @@ export class ArrayType implements Type {
     return 'array'
   }
   get isEquatable(): boolean {
-    return this.itemType.isEquatable
+    // Defaulted equality on structs containing std::vector can make Swift drop
+    // the vector's CxxRandomAccessCollection conformance (swiftlang/swift#67410).
+    // Equality is only a generated convenience, so keep array-bearing structs
+    // non-equatable until the affected Swift toolchains are no longer supported.
+    return false
   }
 
   getCode(language: Language, options?: GetCodeOptions): string {
